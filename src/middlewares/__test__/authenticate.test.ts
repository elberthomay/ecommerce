// it("should throw AuthenticationError if token is invalid");
// it("should throw AuthenticationError if sign is invalid");
// it("should throw AuthenticationError if token has expired");
// it("should throw AuthenticationError if token is not yet valid");
// it("should set Request.tokenData as null if force is false");
// it("should set Request.tokenData with data of TokenType if token is valid");
