apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecommerce-db-pvc
spec:
  storageClassName: ibmc-vpc-block-retain-5iops-tier
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
