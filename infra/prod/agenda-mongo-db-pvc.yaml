apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agenda-mongo-db-pvc
spec:
  storageClassName: do-block-storage-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
