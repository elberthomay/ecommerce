apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agenda-mongo-db-pvc
spec:
  storageClassName: managed-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
